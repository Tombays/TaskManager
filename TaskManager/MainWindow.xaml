<Window x:Class="TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TaskManager"
        Title="Task Manager" Height="680" Width="520"
        MinHeight="500" MinWidth="420"
        Background="#0F0F13"
        FontFamily="Segoe UI"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <!-- Converters -->
        <local:BoolToStrikethroughConverter x:Key="StrikeConverter"/>
        <local:BoolToOpacityConverter x:Key="OpacityConverter"/>

        <!-- ScrollBar Style -->
        <Style x:Key="MinimalScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Rectangle RadiusX="2" RadiusY="2" Fill="#3A3A4A"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MinimalScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="6"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar Grid.Column="1"
                                       Style="{StaticResource MinimalScrollBar}"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style -->
        <Style x:Key="TaskCheckBox" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="22" Height="22">
                            <Ellipse x:Name="OuterRing"
                                     Stroke="#3A3A52" StrokeThickness="1.5"
                                     Fill="Transparent"/>
                            <Ellipse x:Name="InnerDot"
                                     Width="10" Height="10"
                                     Fill="#6C63FF"
                                     Opacity="0"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                            <Path x:Name="Checkmark"
                                  Data="M6,11 L10,15 L16,7"
                                  Stroke="#6C63FF" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="#6C63FF"/>
                                <Setter TargetName="Checkmark" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="#6C63FF"/>
                                <Setter TargetName="OuterRing" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="BG" Fill="Transparent"/>
                            <Path Data="M8,8 L16,16 M16,8 L8,16"
                                  Stroke="#FF5C7A" StrokeThickness="1.5"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BG" Property="Fill" Value="#1FFF5C7A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Button Style -->
        <Style x:Key="AddButton" TargetType="Button">
            <Setter Property="Background" Value="#6C63FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#7B74FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#5A52E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="TaskInput" TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A24"/>
            <Setter Property="Foreground" Value="#E8E8F0"/>
            <Setter Property="CaretBrush" Value="#6C63FF"/>
            <Setter Property="BorderBrush" Value="#2A2A3A"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#6C63FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4A4A62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView Style -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- Background accent -->
        <Ellipse Width="300" Height="300"
                 HorizontalAlignment="Right" VerticalAlignment="Top"
                 Margin="0,-100,-80,0"
                 Opacity="0.04">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#6C63FF" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Grid Margin="28,24,28,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="ЗАДАЧИ"
                               Foreground="#6C63FF"
                               FontSize="10"
                               FontWeight="Bold"
                               LetterSpacing="3"
                               Opacity="0.8"/>
                    <TextBlock Text="Task Manager"
                               Foreground="#E8E8F0"
                               FontSize="26"
                               FontWeight="Bold"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Stats Badge -->
                <Border Grid.Column="1"
                        Background="#1A1A24"
                        BorderBrush="#2A2A3A"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="14,10"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" x:Name="StatsPanel">
                        <StackPanel Margin="0,0,14,0">
                            <TextBlock x:Name="TotalCount"
                                       Text="0"
                                       Foreground="#E8E8F0"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="всего"
                                       Foreground="#555568"
                                       FontSize="9"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Rectangle Width="1" Fill="#2A2A3A" Margin="0,2"/>
                        <StackPanel Margin="14,0,14,0">
                            <TextBlock x:Name="ActiveCount"
                                       Text="0"
                                       Foreground="#6C63FF"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="активных"
                                       Foreground="#555568"
                                       FontSize="9"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Rectangle Width="1" Fill="#2A2A3A" Margin="0,2"/>
                        <StackPanel Margin="14,0,0,0">
                            <TextBlock x:Name="DoneCount"
                                       Text="0"
                                       Foreground="#3ED6A0"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="готово"
                                       Foreground="#555568"
                                       FontSize="9"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Divider -->
            <Rectangle Grid.Row="1" Height="1" Fill="#1E1E2C" VerticalAlignment="Center"/>

            <!-- Input Area -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TaskInputBox"
                         Grid.Column="0"
                         Height="46"
                         Style="{StaticResource TaskInput}"
                         KeyDown="TaskInputBox_KeyDown">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <!-- Placeholder -->
                <TextBlock Grid.Column="0"
                           Text="Новая задача..."
                           Foreground="#3A3A52"
                           FontSize="13.5"
                           Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=TaskInputBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Grid.Column="2"
                        Height="46"
                        Style="{StaticResource AddButton}"
                        Content="+ Добавить"
                        Click="AddTask_Click"/>
            </Grid>

            <!-- Progress Bar -->
            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#141420"
                        CornerRadius="4"
                        Height="4">
                    <Border x:Name="ProgressBar"
                            Background="#6C63FF"
                            CornerRadius="4"
                            HorizontalAlignment="Left"
                            Width="0">
                        <Border.Effect>
                            <DropShadowEffect Color="#6C63FF" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                        </Border.Effect>
                    </Border>
                </Border>

                <TextBlock Grid.Row="2"
                           x:Name="ProgressText"
                           Text=""
                           Foreground="#3A3A52"
                           FontSize="10"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- Task List -->
            <Border Grid.Row="6"
                    Background="#13131C"
                    BorderBrush="#1E1E2C"
                    BorderThickness="1"
                    CornerRadius="14"
                    ClipToBounds="True">
                <Grid>
                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyState"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Opacity="0.4">
                        <TextBlock Text="✓"
                                   Foreground="#6C63FF"
                                   FontSize="36"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="Список задач пуст"
                                   Foreground="#6666AA"
                                   FontSize="14"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Добавьте первую задачу выше"
                                   Foreground="#44445A"
                                   FontSize="11"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <ListView x:Name="TaskListView"
                              Margin="0"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.Template>
                            <ControlTemplate TargetType="ListView">
                                <ScrollViewer Style="{StaticResource MinimalScrollViewer}"
                                              Focusable="False">
                                    <ItemsPresenter Margin="12,8,12,8"/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ListView.Template>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:TaskItem}">
                                <Border x:Name="TaskCard"
                                        Background="#1A1A26"
                                        BorderBrush="#222234"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Margin="0,4,0,4"
                                        Padding="14,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Checkbox -->
                                        <CheckBox Grid.Column="0"
                                                  Style="{StaticResource TaskCheckBox}"
                                                  IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                  VerticalAlignment="Center"/>

                                        <!-- Task Info -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="#D8D8E8"
                                                       FontSize="13.5"
                                                       TextWrapping="Wrap"
                                                       Opacity="{Binding IsCompleted, Converter={StaticResource OpacityConverter}}"
                                                       TextDecorations="{Binding IsCompleted, Converter={StaticResource StrikeConverter}}"/>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd MMM yyyy, HH:mm}'}"
                                                       Foreground="#3A3A52"
                                                       FontSize="10"
                                                       Margin="0,3,0,0"/>
                                        </StackPanel>

                                        <!-- Delete Button -->
                                        <Button Grid.Column="3"
                                                x:Name="DeleteBtn"
                                                Style="{StaticResource DeleteButton}"
                                                VerticalAlignment="Center"
                                                Tag="{Binding}"
                                                Click="DeleteTask_Click"/>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="TaskCard" Property="BorderBrush" Value="#2E2E48"/>
                                        <Setter TargetName="DeleteBtn" Property="Opacity" Value="1"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                        <Setter TargetName="TaskCard" Property="Background" Value="#15151F"/>
                                        <Setter TargetName="TaskCard" Property="BorderBrush" Value="#1A1A28"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
